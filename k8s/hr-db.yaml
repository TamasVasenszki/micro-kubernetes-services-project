apiVersion: apps/v1
kind: Deployment
metadata:
  name: hr-db-deployment
spec:
  replicas: 1
  selector:
    matchLabels:
      app: hr-db
  template:
    metadata:
      labels:
        app: hr-db
    spec:
      containers:
        - name: hr-db-container
          image: postgres:15
          ports:
            - containerPort: 5432
          env:
            - name: POSTGRES_USER
              valueFrom:
                secretKeyRef:
                  name: app-secrets
                  key: HR_DB_USER
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: app-secrets
                  key: HR_DB_PASSWORD
            - name: POSTGRES_DB
              valueFrom:
                secretKeyRef:
                  name: app-secrets
                  key: HR_DB_NAME
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "250m"
              memory: "256Mi"

---
apiVersion: v1
kind: Service
metadata:
  name: hr-db-service
spec:
  selector:
    app: hr-db
  ports:
    - protocol: TCP
      port: 5432
      targetPort: 5432
  type: ClusterIP